<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twentytwo.ad.train">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ticket" />
        <activity android:name=".Asking_two" />
        <activity android:name=".Asking" />
        <activity android:name=".Check_Info" />
        <activity android:name=".Reservation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sign_in" />
        <activity android:name=".Sign_up" />
        <activity android:name=".ReservationAsking" />
    </application>

</manifest>